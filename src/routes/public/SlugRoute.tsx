// `/:slug` resolves to an article or a category. For now this reuses the
// existing client-side Home logic; the SSR loader + meta() land in the next unit.
export { default } from "@/pages/frontend/Home";
