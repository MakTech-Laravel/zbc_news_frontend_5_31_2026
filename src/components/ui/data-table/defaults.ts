import type { DataTableDefaults } from "@/components/ui/data-table/types";

export const DATA_TABLE_DEFAULTS: DataTableDefaults = {
  emptyMessage: "No results found.",
  actionsColumnHeader: "Actions",
  minWidth: "900px",
  selectable: false,
};
